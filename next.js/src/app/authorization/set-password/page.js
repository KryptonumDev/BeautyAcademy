import SetPassword from "@/components/sections/set-password";

const PasswordChangePage = async () => {
  // TODO: Add check if user is logged in
  return <SetPassword />;
};

export default PasswordChangePage;
